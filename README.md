# C1-Projects
The FindLargestSum project gets the largest possible sum that can be obtained from a pair of values in the list without adding the same number to itself.

The FindMostCommonChar project finds the most common character in a string array using a HashMap to keep track of how many of each character there are.

The Fibonacci project uses recursion to find the fibonacci result given an n input.